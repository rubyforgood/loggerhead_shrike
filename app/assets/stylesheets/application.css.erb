/*
 * This is a manifest file that'll be compiled into application.css, which will include all the files
 * listed below.
 *
 * Any CSS and SCSS file within this directory, lib/assets/stylesheets, vendor/assets/stylesheets,
 * or any plugin's vendor/assets/stylesheets directory can be referenced here using a relative path.
 *
 * You're free to add application-wide styles to this file and they'll appear at the bottom of the
 * compiled file so the styles you add here take precedence over styles defined in any other CSS/SCSS
 * files in this directory. Styles in this file should be added after the last require_* statement.
 * It is generally better to create a new file per style scope.
 *
 *= require_tree .
 *= require_self
 */

.about_image {
  height: 350px;
  width: 350px;
  background-image: url(<%= asset_path("shrike_on_a_stick.jpg") %>);
  border-radius: 50%;
  border: none;
  background-position: ;
}

.meet_image {
  height: 500px;
  width: 500px;
  border-radius: 50%;
  background-image: url(<%= asset_path("shrike_flying.jpg") %>);
  background-position: 50% 20%;
}

.habitat_image {
  height: 400px;
  width: 400px;
  border-radius: 50%;
  background-image: url(<%= asset_path("alvar_habitat.jpg") %>);
}

.conservation_image {
  height: 400px;
  width: 400px;
  border-radius: 50%;
  background-position: 0% 25%;
  background-image: url(<%= asset_path("loggerhead_shrike_grumpy.jpg") %>);
}

.outreach_image {
  height: 350px;
  width: 350px;
  border-radius: 50%;
  background-image: url(<%= asset_path("bird_watchers.jpg") %>);
}

.jumbotron {
  background-image: url(<%= asset_path("landscape.jpg") %>);
  height: 300px;
}

.jumbotron h1 {
  color: #efefef;
  font-family: times;
  font-size: 38px;
}
